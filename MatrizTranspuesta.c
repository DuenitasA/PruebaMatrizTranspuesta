#include <stdio.h>
#include <time.h>
#include <stdlib.h>


int main(void){

    srand(time(NULL));
    int a, b;

    printf("Ingrese la cantidad de filas.\n");
    scanf("%d", &a);
    printf("Ingrese la cantidad de columnas.\n");
    scanf("%d", &b);

    int matriz[a][b];

    for (int i = 0; i < a; i++)
    {
        for (int j = 0; i < b; j++)
        {
            matriz[a]
        }
        
    }
    


    return 0;
}